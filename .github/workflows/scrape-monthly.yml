name: Scrape mensuel des données Bolkiri

on:
  schedule:
    # 1er de chaque mois à 2h du matin
    - cron: '0 2 1 * *'
  workflow_dispatch: # Permet lancement manuel

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install requests beautifulsoup4
    
    - name: Run scraper
      run: python scraper_industrial_2025.py
    
    - name: Commit and push if changed
      run: |
        git config user.name github-actions
        git config user.email github-actions@github.com
        git add bolkiri_knowledge_industrial_2025.json
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Mise à jour mensuelle KB" && git push)
